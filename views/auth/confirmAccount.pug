extends ../layout/index


block content

    .py-10
        h1.text-4xl.my-10.font-extrabold.text-center Real
            span.font-normal Estate

        h2.text-center.text-2xl.font-extrabold= page

    .bg-white.shadow.py-8.px-4.rounded-lg.max-w-md.mx-auto
        p( class= error ? 'bg-red-500' : 'bg-green-500' ).py-2.px-5.rounded-lg.w-full.font-bold.text-center.text-white.uppercase= message

        if !error
            a(href="/auth/login").text-center.font-bold.text-sm.text-slate-600.uppercase.mt-10.block You Can Login